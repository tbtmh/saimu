<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chatbot</title>
    <link rel="stylesheet" href="../public/css/styles.css">
</head>
<body>
    <div class="chat-container">
        <h2>Temple Chatbot</h2>
        <div id="chat-box" class="chat-box">
            <% if (messages.length > 0) { %>
                <% messages.forEach(message => { %>
                    <p><strong>You:</strong> <%= message.user %></p>
                    <p><strong>Bot:</strong> <%= message.bot %></p>
                <% }) %>
            <% } else { %>
                <p>Welcome! Ask me about temple recommendations.</p>
            <% } %>
        </div>

        <form action="/chatbot" method="POST">
            <input type="text" name="message" id="user-input" placeholder="Ask me anything..." required>
            <button type="submit">Send</button>
        </form>
    </div>
</body>
</html>
